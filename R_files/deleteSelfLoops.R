#I get read of the loops to make the graph more clear
#edge_list_loopless <- edge_list
#for (i in 1:nrow(edge_list_loopless)){
#  if (edge_list[i,1]==edge_list[i,2]){
#    edge_list_loopless <- edge_list_loopless[-c(i),] 
#  }
#}