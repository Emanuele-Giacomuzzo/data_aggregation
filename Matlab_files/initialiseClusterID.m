function [clusterID_nDC,clusterID_nwDC,clusterID_nCC,clusterID_nBC,clusterID_s,clusterID_cs,clusterID_ns,clusterID_k,clusterID_kbu,clusterID_ktd,clusterID_kdir,clusterID_kindir,clusterID_TI,clusterID_TI_ratio,clusterID_STO,clusterID_STO_ratio,clusterID_TP] = initialiseClusterID(n,a)

clusterID_nDC=zeros(n,a); 
clusterID_nwDC=zeros(n,a); 
clusterID_nCC=zeros(n,a); 
clusterID_nBC=zeros(n,a); 
clusterID_s=zeros(n,a); 
clusterID_cs=zeros(n,a); 
clusterID_ns=zeros(n,a); 
clusterID_k=zeros(n,a);
clusterID_kbu=zeros(n,a); 
clusterID_ktd=zeros(n,a); 
clusterID_kdir=zeros(n,a); 
clusterID_kindir=zeros(n,a); 
clusterID_TI=zeros(n,a); 
clusterID_TI_ratio=zeros(n,a); 
clusterID_STO=zeros(n,a); 
clusterID_STO_ratio=zeros(n,a); 
clusterID_TP=zeros(n,a);

end